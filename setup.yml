---
- name: Setup Docker-Server
  hosts: localhost
  remote_user: root

  tasks:

  - name: Hello Server
    debug:
      msg: "Hello DEVK!!" 
      
  - name: Create an insecure file
    file:
      path: /temp
      owner: root
      group: root
      mode: '1777'
